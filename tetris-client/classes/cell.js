const Cell = (function createCellClass() {

  return class Cell {

    constructor(x, y, piece=null){
      this.x = x;
      this.y = y;
      this.state = {piece:piece, shapeId:null}
    }





  }
})()